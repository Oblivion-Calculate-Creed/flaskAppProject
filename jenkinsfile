pipeline {
  agent any
  stages {
    stage('tests and building') {
      steps {
        sh '/home/ubuntu/practicalProject/scripts/ssh_pytest.sh'
      }
    }
    stage('login and push to dockerhub') {
      steps {
        sh '/home/ubuntu/practicalProject/scripts/docker_push.sh'
      }
    }
  }
}